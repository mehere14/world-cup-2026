services:
  - type: web
    name: world-cup-radial-bracket
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: FOOTBALL_DATA_TOKEN
        sync: false
